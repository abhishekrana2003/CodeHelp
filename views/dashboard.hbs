<h6>Dashboard</h6>

<h3>Welcome {{name}} to CodeHelp</h3>

<p>Share and explore interview-related DSA questions to prepare for your next big opportunity.</p>

{{#if code}}
  <table class="striped">
    <thead>
        <tr>
            <th>Problem</th>
            <th>Date</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each code}}
           <tr>
             <td><a href="/code/{{_id}}">{{title}}</a></td>
             <td>{{formatDate createdAt 'MMMM Do YYYY, h:mm:ss a'}}</td>
             <td><span class="dash-status">{{status}}</span></td>
             <td>
               <a href="/code/edit/{{_id}}" class="btn btn-float">
                 <i class="fas fa-edit"></i>
                </a>

                 <form action="/code/{{_id}}" method="POST" id="delete-form">
                   <input type="hidden" name="_method" value="DELETE">
                   <button type="submit" class="btn red">
                    <i class="fas fa-trash"></i>
                   </button>
                 </form>
              
             </td>
           </tr>
        {{/each}}
    </tbody>
  </table>

{{else}}
<p>You haven't posted any questions yet. Let's get started!</p>
{{/if}}