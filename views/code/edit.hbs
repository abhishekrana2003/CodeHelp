<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Question</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <style>
        #editor {
            width: 100%;
            height: 400px;
            border: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <h3>Add Question</h3>
    <div class="row">
        <form action="/code/{{code._id}}" method="POST" class="col s12">
            <input type="hidden" name="_method" value="PUT">
            <div class="row">
                <div class="input-field">
                    <input type="text" id="title" name="title" required value="{{code.title}}">
                    <label for="title">Title</label>
                </div>
            </div>

            <div class="row">
                <div class="input-field">
                    <textarea id="problem" name="problem" class="materialize-textarea" required>{{code.problem}}</textarea>
                    <label for="problem">Problem</label>
                </div>
            </div>

            <div class="row">
                <div class="input-field">
                    <select id="status" name="status" required>
                        {{#select story.status}}
                        <option value="public" selected>Public</option>
                        <option value="private">Private</option>
                        {{/select}}
                    </select>
                    <label for="status">Status</label>
                </div>
            </div>

            <div class="row">
                <div class="input-field">
                    <h5>Input:</h5>
                    <textarea id="input" name="input" class="materialize-textarea" required>{{code.input}}</textarea>
                </div>
            </div>

            <div class="row">
                <div class="input-field">
                    <h5>Expected Output:</h5>
                    <textarea id="output" name="output" class="materialize-textarea" required>{{code.output}}</textarea>
                </div>
            </div>

        <div class="row">
            <div class="input-field">
                <h5>Code goes here:</h5>
                <textarea id="body" name="body">{{code.body}}</textarea>
            </div>
        </div>

            <div class="row">
                <input type="submit" value="Save" class="btn" onclick="syncEditorContent()">
                <a href="/dashboard" class="btn orange">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>
